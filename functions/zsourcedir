#!/bin/zsh

zsourcedir() {
  local dir zdir file relfile
  for dir in $*; do
    zdir=`zdirname $dir`
    zlog "zsourcedir: dir=$dir, zdir=$zdir"

    for file in $dir/*.zsh(N); do
      zlog "zsourcedir: file=$file"
      relfile=${file#$ZCONFIGDIR}
      zsource $relfile
    done
  done
}
